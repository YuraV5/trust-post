apiVersion: 1

# Grafana datasource provisioning.
# uid is referenced by dashboard JSON panels.
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
